<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.middlemanv007">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launch_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launch_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiddleManv007">
        <activity
            android:name=".AuthorizedUserProfile"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".AuthorizedUserRequestsView"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".AuthorizedCreateRequest"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".AuthorizedUserRegistration"
            android:label="Authorized User Registration" />
        <activity
            android:name=".AuthorizedUserLogin"
            android:label="Authorized User Login" />
        <activity
            android:name=".ForgetPassword"
            android:label="Forget Password" />
        <activity
            android:name=".SalesMela"
            android:label="@string/title_activity_sales_mela"
            android:theme="@style/Theme.MiddleManv007.NoActionBar" />
        <activity
            android:name=".UserRegistration"
            android:label="User Registration" />
        <activity
            android:name=".MainActivity"
            android:label="Login Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>